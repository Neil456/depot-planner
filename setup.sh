#!/usr/bin/env bash
# Environment setup script for Claude Code on the web (paste into the environment's setup script field).
set -e
pip install numpy scipy matplotlib imageio pyyaml pandas pytest pybind11 scikit-build-core
